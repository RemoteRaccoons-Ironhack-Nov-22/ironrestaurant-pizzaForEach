<h1>List of pizzas in our DB:</h1>

{{#if pizzasArr}}
    {{#each pizzasArr}}
        <section class="pizza-summary">
            <h2>{{this.title}}</h2>
            <h3>Price: {{this.price}}€</h3>

            <a href="/pizzas/{{this.title}}">More details</a>

            <hr>
        </section>
    {{/each}}
{{else}}
    <h3>Sorry, no pizzas with that search criteria</h3>
{{/if}}
